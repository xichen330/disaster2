<%= form_for @message, :url => { :controller => 'events', :action => 'leave_msg' } do |f| %>

    <%= f.label :name, "姓名" %>
    <%= f.text_field :name %>

    <%= f.label :messages, "訊息" %>
    <%= f.text_area :messages %>

    <%= f.submit "Leave" %>
<% end %>