<%= form_for @message, :url => event_messages_path(@event) do |f| %>

    <%= f.label :name, "姓名" %>
    <%= f.text_field :name %>

    <%= f.label :text, "訊息" %>
    <%= f.text_area :text %>

    <%= f.submit "Leave" %>
<% end %>